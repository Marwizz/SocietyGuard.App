import { View, Text } from 'react-native'
import React from 'react'

export default function GroupPreapprove({ route }) {

    const { societyId } = route.params;

  return (
    <View>
      <Text>GroupPreapprove</Text>
    </View>
  )
}